import 'dart:typed_data';

class PdfUtils {
  // Métodos vacíos para evitar errores de compilación en Web
  static Future<void> openFile(Uint8List bytes, String filename) async {
    // No soportado en Web (sin path_provider ni open_filex)
  }
}